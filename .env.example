MONGO_URI=
MONGO_DB_NAME=
PORT=8003

# Copia este archivo como ".env" y llena con tus valores reales.
# NUNCA hardcodear "localhost" en el código; en producción la BD vive en otra VM.

# Ejemplo local (mientras pruebas en tu PC / WSL con MongoDB en Docker o local):
# MONGO_URI=mongodb://localhost:27017
# MONGO_DB_NAME=ludoteca_membresias

